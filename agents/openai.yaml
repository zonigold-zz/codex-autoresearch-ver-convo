interface:
  display_name: "Codex Autoresearch"
  short_description: "Single-entry runtime-managed experimentation"
  brand_color: "#2563EB"
  default_prompt: "Use $codex-autoresearch as the single human entrypoint: infer a goal, metric, scope, and verify/guard commands for this repo, confirm once, then launch the runtime-managed loop. Reuse the same skill for status, stop, or resume requests."

policy:
  allow_implicit_invocation: true
