approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "cached"

[sandbox_workspace_write]
network_access = false

[features]
codex_hooks = false

[windows]
sandbox = "elevated"
# sandbox = "unelevated" # fallback if elevated setup fails
